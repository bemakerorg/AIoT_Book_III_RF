#ifndef MODEL_H_
#define MODEL_H_

// Il modello addestrato su Colab, trasformato in codice sorgente.
// Questi due nomi sono gli stessi che la cella Python del notebook scrive dentro model.cpp:
// se cambiate uno, dovete cambiare anche l'altro.
//
// Questo file e' identico in tutti i progetti del libro e NON si rigenera quando cambia il
// modello: contiene soltanto i due nomi, che restano sempre gli stessi. Dal notebook si
// scarica il solo model.cpp.
extern const unsigned char g_model[];
extern const int g_model_len;

#endif  // MODEL_H_
